import { styled } from '@mui/material/styles';

const StyleMessageAutoLineBreak = styled('pre')({
    whiteSpace: 'pre-wrap',
    wordWrap: 'break-word',
});

export default StyleMessageAutoLineBreak;