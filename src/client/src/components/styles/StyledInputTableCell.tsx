import { styled } from '@mui/material/styles';
import StyledTableCell from './StyledTableCell';

const StyledInputTableCell = styled(StyledTableCell)({
    width: '100%',
}) as typeof StyledTableCell;

export default StyledInputTableCell;