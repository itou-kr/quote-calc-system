import Box from '@mui/material/Box';
import { styled } from '@mui/material/styles';

const StyledDataGridSizeControlArea = styled(Box)({
    display: 'flex',
    flexDirection: 'column',
}) as typeof Box;

export default StyledDataGridSizeControlArea;